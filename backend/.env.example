# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=db_user
DB_PASSWORD=db_password
DB_DATABASE=ewallet

# JWT Configuration
JWT_SECRET=your_super_secure_jwt_secret_key_here
# JWT_EXPIRES_IN=1h     # Production (shorter expiration)
JWT_EXPIRES_IN=7d       # Development (longer expiration for convenience)

# Server Configuration
PORT=3001
NODE_ENV=development

# Email Configuration
# For Gmail SMTP (recommended for production):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-gmail@gmail.com
# SMTP_PASS=your-app-password  # Generate App Password from Google Account settings

# For testing with Ethereal Email (development only):
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=abagail.weber@ethereal.email
SMTP_PASS=tGqHkCjH8M5d2mWm3V

# File Upload Configuration
UPLOAD_DIR=uploads
MAX_FILE_SIZE=5242880
